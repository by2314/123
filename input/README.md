# Input Directory

Place your APK files here for local hardening.

Example:
```
input/
├── app.apk
├── myapp-release.apk
└── sample.apk
```

**Note:** APK files are excluded from git by default (see `.gitignore`).

You can also provide URLs to APKs when running the workflow, instead of using local files.
